# AGENTS.md — drop-auth-ldap

LDAP/Active Directory authentication connector plugin for Drop (#230).

## Commands

```sh
pnpm install
pnpm build
pnpm test
```

Built on `@droposs/plugin-sdk` (plugin API v2); the dependency resolves the
sibling checkout at `../drop-plugin-sdk/packages/plugin-sdk`.
